node_modules/
dist/
coverage/
*.tsbuildinfo
*.tgz
.DS_Store
.vscode/
.idea/

# Resolution shims for `market-hours/<venue>`, written by build:calendars.
# Build output, not source — see scripts/build-calendars.mjs.
/xlon/
/rns/

# Large image sources and derived uploads. Only the WebP the README renders is
# committed; these are regenerable from it and are megabytes apiece.
#   cover.png          the generated original
#   social-preview.png 1280x640, uploaded to Settings > Social preview by hand,
#                      which does not accept WebP
assets/*.png
assets/*.jpg
