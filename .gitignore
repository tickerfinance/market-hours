node_modules/
dist/
coverage/
*.tsbuildinfo
*.tgz
.DS_Store
.vscode/
.idea/

# Resolution shims for `market-hours/<venue>`, written by build:calendars.
# Build output, not source — see scripts/build-calendars.mjs.
/xlon/
/rns/

# The generated source PNG is 1.9 MB and visually identical to the committed
# WebP. Kept out of history deliberately; it is also what you upload to
# Settings > Social preview, which does not accept WebP.
assets/cover.png
